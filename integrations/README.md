# MCP-Bastion integrations

Provider- and framework-specific READMEs live in each `mcp-bastion-*` subdirectory (OpenAI, Anthropic, LangChain, FastMCP, etc.).

**Enterprise stack (Python):** multi-tenant routing, tamper-evident audit chain, OPA/Cedar policy, FinOps attribution, sensitive-content classifier, red-team CLI, and the live dashboard are documented in the repo root [README.md](../README.md) and [docs/FEATURES.md](../docs/FEATURES.md).

When you add a new integration package, link back to those docs so users discover the full Bastion surface area, not only the LLM client snippet.
